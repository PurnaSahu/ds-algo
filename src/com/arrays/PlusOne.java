package com.arrays;

import java.util.Scanner;

public class PlusOne {

	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		  System.out.print("Please enter the size of the array: ");
		/*public int[] plus(int[] digits) {
			int size= digits.length;
			//digits[size-1] =1+ digits[size-1];
			//return
			int sumValue=0;
			for (int i=0;i<size;i++){
				sumValue= (sumValue * 10) + digits[i];
			}
			int outputValue= sumValue+1;

			String value= Integer.toString(outputValue);
			int newSize=value.length();

			int[] myArray = new int[newSize];

			for (int j=newSize-1;j>=0;j--){
				// int j=size-1;
				// while(outputValue>0){
				myArray[j]= outputValue % 10;
				outputValue= outputValue/10;
			}
			return myArray;

		}*/

	}

}
